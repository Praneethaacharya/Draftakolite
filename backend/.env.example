# Environment Variables Template for Backend
# Copy this file to .env and fill in your actual values

# Application Environment
NODE_ENV=production

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=5000

# Database Connection
# Get your MongoDB connection string from: https://cloud.mongodb.com
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/akolite?retryWrites=true&w=majority

# SSL/TLS Certificates (Optional - for direct HTTPS on EC2)
# Leave empty if using CloudFront for HTTPS
SSL_KEY_PATH=/etc/ssl/private/server.key
SSL_CERT_PATH=/etc/ssl/certs/server.crt

# JWT Configuration (Optional)
JWT_SECRET=your_secret_key_here_min_32_characters_long

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Frontend URL (for CORS)
FRONTEND_URL=https://akoliteresin.github.io/akoliteFrontEnd

# CloudFront Configuration (for HTTPS)
CLOUDFRONT_URL=https://dj4haaiis0la7.cloudfront.net

# API Configuration
API_BASE_URL=https://dj4haaiis0la7.cloudfront.net

# Notes:
# 1. Never commit this file with real values to git
# 2. Keep this file secure and use .gitignore
# 3. For production, use environment variable management service
# 4. MongoDB: Create free account at https://cloud.mongodb.com
# 5. Update CORS_ORIGINS in server.js if adding new domains
